## **Основы NumPy: массивы и векторные операции**
---
[<--](../labs.md)
#### **Задачи**
- изучить библиотеки *numpy, pandas и тд*;

- реализовать функции (вычисления с векторами, матрицами);

- реализовать вывод гистограмм, heatmap;

- написать тесты (*pytest*).

#### **Код**
[ссылка на репозиторий в Github](https://github.com/mimi-star12/labs_python-2/tree/main/lab2.numpy) (lab2)

---

### **Выводы:**
во время работы столкнулась с проблемой большого количества новых команд, к семантике которых предстоит привыкнуть. также запуск тестов не получался какое-то время, т.к. команду **pytest test.py -v** нужно было вызывать непосредственно в папке, где лежит файл с тестами (*test.py*). 

результатом работы стал проект, состоящий из файла с множеством функций, которые подойдут для анализа некоторого массива данных, а также тестов, которые вышеупомянутые функции успешно проходят