## **Рекурсивные и итеративные деревья**

---
[<--](../labs.md)
#### **Задание:**
Сравните время работы двух реализаций функции построения бинарного дерева:

- ***рекурсивной*** — дерево строится путём рекурсивных вызовов для левого и правого поддерева

- ***нерекурсивной*** — через цикл и явную структуру данных, например очередь или стек


**Требуется:**

Реализовать внутри одного файла две функции:

- build_tree_recursive(data)

- build_tree_iterative(data)

Сравнить их время работы с помощью timeit.

Построить график (ось X — высота дерева, ось Y — время построения дерева).

Сделать выводы о сравнении двух подходов.

#### **Код:**
[ссылка на репозиторий в GitHub](https://github.com/mimi-star12/labs-python/tree/main/lab6) (lab6)

---

### **Выводы:**
рекурсивный подход работает быстрее на небольших высотах дерева, 
но при увеличении высоты итеративный подход становится предпочтительнее